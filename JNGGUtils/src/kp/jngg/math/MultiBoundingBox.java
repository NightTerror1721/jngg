/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package kp.jngg.math;

/**
 *
 * @author Asus
 */
public class MultiBoundingBox extends BoundingBox
{
    /*private BoundingBox[] bboxs;
    
    public MultiBoundingBox(BoundingBox... innerBoundingBoxs)
    {
        if(innerBoundingBoxs == null)
            throw new NullPointerException();
        this.bboxs = innerBoundingBoxs;
    }
    
    public final int getBoundingBoxCount() { return bboxs.length; }
    public final BoundingBox getBoundingBox(int index) { return bboxs[index]; }
    public final void setBoundingBox(int index, BoundingBox innerBoundingBox) { bboxs[index] = Objects.requireNonNull(innerBoundingBox); }
    public final void setAllBoundingBox(BoundingBox... innerBoundingBoxs)
    {
        if(innerBoundingBoxs == null)
            throw new NullPointerException();
        this.bboxs = innerBoundingBoxs;
    }
    
    private void resituateInner(BoundingBox bbox, double oldX0, double oldY0, double oldX1, double oldY1)
    {
        
    }*/
}
